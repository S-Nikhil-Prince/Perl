#!/usr/bin/perl =>Interpreter directive to specify that this script should be run using Perl.
use strict; => Enforce strict variable declaration rules, which helps catch errors
use warnings; => Enable warnings to alert the programmer of potential issues in the code
=start 
print "VLSI,Perl";
print "VLSI,Perl";
=cut
print "VLSI,Perl \n";
if(1==1){
    print "Nikhil Prince \n";
    print "Nikhil Prince \n";
}

