#!/usr/bin/perl     =>interpretation directive
use strict;       =>enforce strict variable declaration  -|
use warnings;     =>enable warnings for potential issues _|-> compiler directives
print "VLSI - Perl";

to comment sinlge line add hash before the line
 and if we want to commenr multiple lines write lines between 
 =start
 statements;
 =cut

if condition 
if(condition){
    statement;
}


$ => Scalar Variable => holds single value or string or numbers.
@ => Vector Variable => holds multiple values
% => Key/Pair (hashes) holds pair of data


my
:to declare any variable.
ex: 
my $age=20;
my $name="Nikhil";

my @namee=("Nikhil","Prince");
my @namee=(1,2,3,4,"Num");

my %person_data()